# Variables publiques Vite (préfixe VITE_ requis pour être accessibles côté client)
# Ne jamais mettre de secrets ici — tout ce qui a VITE_ est embarqué dans le bundle JS

VITE_API_URL=http://localhost:8000
